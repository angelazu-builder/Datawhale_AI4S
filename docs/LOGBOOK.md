# 探索日志 / Exploration Logbook
## Phase Transitions in Wealth Inequality — Kesten AI Research Engine

**项目**: Phase Transitions in Wealth Inequality: an Active Learning Approach on Kesten Stochastic Dynamics  
**阶段**: 初赛 (v1.1.0) | **最后更新**: 2026-08-16

---

## 📍 当前探索状态

| 维度 | 状态 |
|------|------|
| 核心模拟引擎 | ✅ N=100,000, T=500，Numba JIT + NumPy 降级，运行稳定 |
| 主动学习智能体 | ✅ 30 轮 UCB+GP 探索完成，等预算随机对照组已建立 |
| 相变图（D1）| ✅ 控因 1D 扫描完成（固定 c=0.10, reflect, lognormal） |
| 左尾模型选择 | ✅ AIC/BIC 三分布比较完成 |
| 实证校准 | ✅ World Bank API 接入，中美 1980-2023 对齐完成 |
| 反事实政策推演 | ✅ 精准扶贫 S>0、累进税 τ>0 推演完成 |
| 非线性行为扩展 | ✅ Keynesian MPC + Prospect Theory（独立模块）|
| 复杂网络拓扑扩展 | ✅ Scale-Free + Small-World 网络（独立模块）|
| 统计检验 | ⚠️ Welch p ≈ 0.28，AI vs 随机差异尚未达到强显著性 |

---

## 🔍 主要发现

### F1 ｜【预期之外】吸收边界触发系统结构坍塌

在测试边界条件参数时，切换至 `absorb`（财富触底即归零）时，
幂律拟合优度 R² 从 > 0.85 骤降至 < 0.50，幂律分布整体失效。

**这个发现在跑代码之前完全没有预期。** 最初测试 absorb 边界只是把它当作参数空间中一个普通的离散维度，
没有预料到它会触发定性的结构相变。这告诉我们：边界条件不只是数值细节，
而是决定系统相的关键结构参数，值得作为独立的物理问题来研究。

### F2 ｜政策失效存在硬边界（有价值的负结果）

在高冲击风险 p > 0.04 的区域，即使施加 τ = 0.15 的累进税，贫困率仍然无法遏制——甚至反弹。
这意味着纯再分配政策的有效性存在一个由冲击频率决定的硬上限。
与政策经济学中"精准补贴优于税收再分配"的结论定性吻合，但给出了机制解释。

### F3 ｜中美参数结构的定性差异

世界银行数据校准后，中国最优参数呈现高 c（耗散率高，消费/税收强）、低 p（冲击风险低），
而美国则相反——与两国不同的经济体制和社会保障结构定性吻合。

### F4 ｜（尚待确认）相变临界值 R* ≈ 0.15–0.20

控因扫描显示临界区在 R* ≈ 0.15–0.20，但梯度峰值随种子有 ±0.02 波动，
需要更密集扫描和更多验证种子才能给出置信区间。

---

## 👥 来自导师与同行的建议

### 评审 Review（预审评委）

> "R* = p/c 的图不能证明相变——每个点同时改变了多个变量，混杂。"

→ **已响应**：控因 1D 扫描，固定 c/boundary/income_dist，仅沿 p 变化，消除混杂。

> "先证明幂律优于对数正态/其他候选分布，再讨论其指数。"

→ **已响应**：加入 AIC/BIC + Likelihood Ratio Test，三分布系统比较。

> "D1 的跨种子复现逻辑不充分——将当前参数与不同参数组合的均值比较，不等于控制实验。"

→ **已响应**：更新为同一参数下 10 个独立验证种子的相对跳变检验（Δβ > 0.3 vs 邻域均值）。

### 会议口头建议

> "把经济学书上的定性行为模式给 AI，看跑出来什么结果。"

→ **已响应**：`behavioral_simulator.py`——Keynesian MPC c(W)~W^{-α} 和 Prospect Theory p(W)~1/W。

> "清华有一个 AI Agent Society（tsinghua-fib-lab/agentsociety）。"

→ **已记录**：列入参考文献 [E6]，作为下一步多智能体框架的参考架构。

> "把人看成结构，看结构的动力学。AI 时代数学工具不是问题。"

→ **部分响应**：`network_simulator.py`（BA + WS 网络）。连续场/Fokker-Planck 框架留待 v2.0。

---

## 🗺️ 接下来想探索什么

**短期（v1.2.0，决赛前）**

1. **修复统计显著性弱点**：增加预算至 80–100 轮，调优 UCB κ，使用 Bootstrap 重采样替代 Welch's t-test。
2. **精化相变边界**：R* ∈ [0.12, 0.22] 密集扫描（步长 0.005），计算 β_left 一阶导峰值置信区间。
3. **解释 F1 的机制**：对比 reflect vs absorb 下的财富分布动态演化，观察分布类型相变而非仅拟合质量下降。

**中期（v2.0，决赛）**

4. **连续场动力学**：从离散 agent 迁移至 Fokker-Planck 财富密度场方程，AI 学习临界参数下的数值解。
5. **LLM 假说生成**：将 `scientific_signals.json` 喂给 LLM，实现 AI 驱动的假说迭代闭环。
6. **AgentSociety 框架接入**：在清华 FIB-Lab 多智能体框架中实现社会交互的财富动力学，超越独立 agent 假设。

---

## 📝 方法论反思

这次最重要的教训：**问题定义的精确性比模型复杂性更重要。**

早期的"相变图"将 p、c、边界条件、收入分布同时变化后压成 R* 比值——
看起来像相变图，实际上是混杂实验。评委一针见血。修复它不需要更复杂的模型，
只需要更严格的实验控制。

**AI 可以比人更高效地搜索参数空间，但它无法替代人对实验设计的把关。**

---
*Supervisor: Angela Zu | AI Research Engine v1.1.0 | 2026-08-16*
